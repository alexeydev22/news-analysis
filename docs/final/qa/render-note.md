# Render QA Note

Автоматическая структурная проверка Office-файлов и визуальная проверка DOCX
выполнены:

- `.docx` открывается как OOXML-пакет;
- `.pptx` открывается как OOXML-пакет;
- в `.docx` найдено 101 параграф, 4 таблицы и 2 изображения;
- в `.pptx` найдено 12 слайдов и 3 media-объекта;
- в `.docx` и `.pptx` найдены ML/NLP и backend-разделы;
- compose config, backend tests, frontend tests и проверка OOXML-архивов
  выполнены успешно;
- `.docx` отрендерен через LibreOffice и Poppler в `docs/final/qa/docx-render`;
- свежий экспорт `.pptx` через LibreOffice не выполнен: локальный `soffice`
  завершился с `Abort trap: 6`. Целостность PPTX проверена через OOXML-архив.

По результатам просмотра DOCX contact sheet и структурной проверки PPTX:

- титульные данные отображаются в `.docx`;
- титульные данные присутствуют в структуре `.pptx`;
- нумерация разделов и списков в `.docx` начинается корректно;
- презентация содержит 12 слайдов и закрывает критерии оценки;
- таблицы, схемы и иллюстрация интерфейса в `.docx` читаются без явных
  наложений.

Перед финальной сдачей рекомендуется открыть:

- `docs/final/coursework-explanatory-note.docx`;
- `docs/final/coursework-defense-presentation.pptx`;

и проверить титульные данные. В документы внесены: Прудиев Алексей Сергеевич,
ПМ23-4, Финансовый университет при Правительстве РФ. Остается заполнить ФИО
руководителя и, при необходимости, название дисциплины.
